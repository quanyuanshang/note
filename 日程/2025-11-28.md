- [x] 1. 调整AI project reward
> 具体详细看看episode啥意思，为啥训练中间会断掉说list out of range？
> get target：TransferStats 用于将一只宠物的属性（攻击力/生命值）复制到另一只宠物。
> 在 TransferStats 中需要两次调用 get_target：
> 什么是episode：Episode 是强化学习中的一次完整交互，从开始到结束。

在 Super Auto Pets 中：

- 开始：调用 env.reset() 时

- 进行：通过多次 env.step(action) 执行动作

- 结束：满足以下任一条件时

1. 赢得 10 场战斗（wins >= 10）

2. 生命值归零（lives <= 0）

3. 达到 25 回合上限（turn >= 25）
> 对其指标。
> for i in range(1024):  # 循环1024次
    obs, reward, done, info = env.step(action)
    if done:
        obs = env.reset()  # 重置，但继续循环
    rewards.append(reward)  # 每步都记录
    只是进行了1024步而已。1. rewards 列表记录了每一步的奖励，不是每一局的奖励

4. len(rewards) = 1024 表示有 1024 个步骤，不是 1024 局游戏

5. 无法直接看出胜利局数

6. 第一次：找到接收属性的宠物（to 目标）

7. 第二次：找到提供属性的宠物（from 来源）
当 get_target 找不到符合条件的宠物时（例如队伍中没有符合条件的宠物，或它们都已死亡），会返回空列表 []。代码直接访问 temp_from[0][0] 就会触发 IndexError。
> 训练断掉重新开始之后还保留了之前的训练状态吗（看到rew_mean在上升） 是
> 周末群里反应一下目前的发现
- [x] 2. 数据可视化PPT 
> 
- [ ] 3.
> 
- [ ] 4.
> 
- [ ] 5.
> 
- [ ] 6.
> 