顶点、边、全局信息都用向量表示
图片用邻接矩阵表示成图
文本用序列连接成图
- Node-level task
判断每个node的属性
- Edge-level task
预测边的属性
![[image-6.png|判断人物之间是什么关系]]
挑战：如何将图的connectivity放入神经网络？![[image-7.png|第i项表示第i条边，两个数字分别表示连接的点、这种方式不会受到点的顺序打乱的影响]]

# GNN
![[image-8.png|输入是图、输出是图]]GNN会对边、点全局的向量进行变换但是不会改变connectivity：
![[image-9.png|f是不改变尺寸的MLP]]
![[image-10.png|这个例子是通过一个线性分类器输出分类]]
> 如果说项分类的顶点没有向量怎么办？利用边的向量和全局向量

- pooling:可以是求和、求mean求max![[image-11.png|汇聚了相邻边的向量和全局向量]]
- 同样没有全局向量但是相对全图做预测可以考虑使用全图的顶点向量
总体：![[image-12.png|中间是三层MLP，每一层都有三种不同的属性（前提是不缺失VEU向量信息）]]
问题是：没有使用connectivity。
- 信息传递：![[image-13.png|加上和邻居的向量再进入MLP]]类似于卷积核权重一样的做卷积。但在GCN里，权重对于位置是不敏感的，所以可以用注意力机制来获取每个顶点的权重![[image-15.png]]
- 顶点到边传递+边到顶点传递![[image-16.png]]
- 全局信息传递![[image-17.png|类似于attention]]
技术优化：
因为每一个节点的perceptive field会比较大，中间Forward计算保留的梯度也多，所以取子图分别保存
随机采样、随机游走取子图。