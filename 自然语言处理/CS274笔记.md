一、Tokenization
1. 对于语言中有空格的语言
- Tokenization with regular expressions
- ![[image-1.png]]
![[image.png]]
例1：能成功匹配 U.S.A.（把它们当作一个完整的token，而不是被点拆分成三个字母）。拒绝匹配 UU.S.A（开头没有点）或 I（后面没有点）。
例2：能完美匹配 $12.40 这样的复杂数字组合作为单一token。拒绝 $.4（点前面没有数字，不符合\d+）、1.4.0（有两个小数点，不符合规则）、1\%%（有两个百分号）。
例3：将 ... 识别为一个独立的token，而不是三个单独的句号
例4：红色方括号 [] 在正则中表示“匹配括号内的任意**一个**字符”。这个表达式列出了一长串常见的标点符号
2. 没有空格（监督学习）



Subword tokenization
	![[image-2.png]]


- BPE：
先在训练集里面根据高频相邻字母组合词加入到词表里面，test阶段根据train阶段学习的顺序来组合字母分词。
- Unigram language modeling tokenization
- WordPiece
都包含token learner和segmenter
二、word-normalization
Lemmatization
Stemming






> [!NOTE] 逻辑回归为什么是线性分类器？
> 看决策边界，也就是正样本和负样本概率相等![[image-3.png]]
