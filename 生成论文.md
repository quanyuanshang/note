# MotionStream
![[image-22.png]]
这篇论文想要做出具有实时交互性能的视频生成模型。
整体输入输出：
输入：
1. 文字prompt
2. 首帧图像（手语数字人图像）
3. **流式驱动输入**：也就是控制的轨迹。训练阶段来自于使用点跟踪模型（**CoTracker3**），去跑训练集里的视频。推理阶段用户给定
输出：
实时根据用户给定轨迹而发生调整的视频。比如在生成视频的过程中用户能看到视频效果并通过一些诸如绘制运动轨迹等操作调整视频生成方向

**1. 之前为啥做不成**

> 1 生成视频速度慢
> 2 bidirectional attention 需要完整的序列。用户必须等到整个序列完成才能看到结果

- ==解决方法：先训一个比较慢的bidirectional attention teacher model,再从这个model 脱胎出一个causual attention student model。==
- 为什么还会需要一个causal attention的学生模型：老师太慢了 学生模型借鉴老师的思路踏着前人的脚步走捷径

![[image-53.png]]
**一、老师模型怎么训练的:**
训练数据集：OpenVid-1M synthetic data generated by some model
输入：轨迹数据（从视频中采样2D轨迹）+prompt text + 首帧图像+扩散模型训练所需要的噪声输入（noisy latent）
输出：根据轨迹调整生成的视频
1. 生成轨迹encoding：
	**本文 Track Head 做法（快）：**
	- **轻量级处理：** 输入的轨迹张量只经过简单的 4倍时间压缩 + 1个 1x1卷积层。
	- **直接拼接 (Concat)：** 将处理后的轨迹特征，直接在通道维度（Channel）拼接到视频的 Latent 上。
	    - 原输入：[Batch, Channel, Time, Height, Width]
	    - 新输入：[Batch, Channel + Track_Dim, Time, Height, Width]
	- **极小改动：** 只需要修改 DiT 的第一层（Patchifying Layer）让它能接受更多通道，后面的大模型结构**完全不用动**。
	- 代价： 几乎为零的额外计算量

> [!NOTE] DiT
>DiT（Diffusion Transformer）是一类基于 Transformer 架构的扩散模型，它用 Transformer 替代了传统扩散模型中的 U-Net 结构，在图像生成任务上展现出更好的可扩展性和性能

> 手语：训练阶段手语视频开头图片/比较完整的照片 轨迹用骨骼点 gt是原本的手语视频

2. 生成平滑运动：Rectified Flow Matching（整流流匹配）

	> 想象你要把一堆 **随机沙子（高斯噪声）** 变成一座 **沙堡（数据分布）**：
	> - **传统扩散**：一粒粒慢慢调整沙子的位置，过程很长。
	> - **Flow Matching**：直接学习一个“力场”，告诉每粒沙子在任意时刻往哪个方向移动。
	> - **Rectified Flow**：更进一步，规定沙子沿着一条直线轨迹从起点走到终点，模型只需学习这条直线上的速度

	这种方法生成的动作通常比传统 Diffusion 更平滑、连贯，非常适合表现手语这种流畅的运动。
	对于手语生成的启发：

3. 处理零值情况：随机掩码stochastic mid-frame masking
	当一个点**没有数据**时（比如手被身体挡住了，或者用户停止了控制），系统通常会传入 **0**（Zero Values）。模型看到 0 时会懵圈——如果模型分不清，就会产生 **Artifacts（伪影）**。比如突然手就生成没了或者凭空出现一些什么东西。
	
	> 对于手语来说会出现这样的问题吗？inference阶段
	
	
	1. **第一阶段（Without Masking）：**
	    - 先给完整的信号，不进行 Masking。
	    - 目标： 建立极强的 **Track-following capability**（听话能力）。
	2. **第二阶段（Fine-tune with Masking）：**
	    - **模拟恶劣环境。** 在微调阶段加入随机 Masking。0.2
	    - 目标： 提高 **Coherence**（连贯性）。当信号断断续续时，让模型学会“脑补”过渡帧，而不是崩坏。




**二、脱胎训练学生模型**

1. 接下来将从老师模型已经训练好的参数出发
==但是对于实时生成长视频会遇到的一些问题：==
> 1误差会累积drift during long-term streaming 
> 2如果视频无限长，
2.  没办法看到未来，causal attention， 但是回头看所有的帧（Full Causal），显存会爆炸：**Sliding-window causal attention**丢弃了中间的历史信息（省显存、提速）。保留了对最近几帧的关注（对角线附近的蓝色/橙色块），这是为了保持动作的连贯性。使用**rolling KV** cache： 只保留最近几帧的KV，随着生成新的画面往后滚。
3. 不跑偏：**Attention sinks**：强制保留了对开头几帧（sink size 帧数不固定增强鲁棒性）的关注【train+inference】![[image-20.png]]
4. 传统来说会保存之前所有的KV，但为了不让显存爆炸，
5. 为了学会将错就错：self-rollout：，在训练学生时，不给标准答案，让学生基于自己画的上一张画，接着画下一张。就算上一帧手画歪了一点点，下一帧也能圆回来，不至于让整个画面崩坏。这是**长视频生成不崩**的关键。
6. - **老师的问题：** 老师太慢了，他必须一步一步解 ODE（走 50 步）才能走到终点。
    - 我们把刚才收集到的那本《老师傅做菜快照集》（ODE Solution Pairs）直接扔给学生模型。
    - **Regression（回归）：** 在机器学习里，回归就是“死记硬背/拟合标准答案”。
    - 学生模型不学怎么“思考”了，它直接看图说话：“哦！如果我看到这块半生不熟的肉（状态 zt），我不需要思考为什么，我直接照着快照集里的答案，加水炖（输出答案）就行了！”
7. Self Forcing distillation (DMD)监督学习：老师每生成一步，都要计算 3 次网络（一次空跑，一次加文本引导，一次加动作引导），然后加权组合。这对实时应用来说太慢了- 通过 DMD 损失函数（公式 3），强制让学生模型的**一次输出结果**，直接去逼近老师模型那**三次计算混合后的结果**。![[image-23.png]]
![[image-24.png]]

> [!NOTE] autoregreesive
> autoregressive model: 它通过一个变量自身过去的数值来预测当前或未来的数值。
- **第一阶段：Causal Adaptation（因果适应预训练）—— 没用 Self-Rollout**
    - 在干嘛： 此时学生模型刚继承老师的参数，刚被蒙上眼睛（变成只能看过去的 Causal 注意力）。
    - 怎么练： 给它看老师生成的完整“标准轨迹（ODE Solution Pairs）”，让它死记硬背（回归训练）。这时候**不用** Self-Rollout，就是简单的一问一答。
        
- **第二阶段：DMD 蒸馏（Distillation）—— 全程使用 Self-Rollout**
    - 在干嘛： 这是为了让模型提速到单步生成，并保证无限长视频不崩的**终极集训**。
    - 怎么练： 模型先连续进行 Self-Rollout（自己接龙画好几个 Chunk），画完一整段后，系统再拿老师的模型（sreal）来给这段完整的“自创视频”打分，计算误差（DMD Loss）并更新参数。

> [!NOTE] 噪声
> - 在扩散模型或自回归模型里，生成过程通常是从噪声逐步还原到数据。每一步生成的 token（比如视频帧的潜在表示）都带有一定的噪声。如果只用“干净的真实 token”训练，模型在推理时遇到自己生成的 noisy token 就可能表现不稳定。所以会有auto regressive roll out。让学生模型在训练时就接触到自己生成的 noisy token，从而学会在真实推理中保持稳定。
    
三、infernce
输入：首帧图像+prompt text +用户输入的轨迹
1. **Denoising Steps
    - 定义： 生成一个 Chunk 需要跑几遍网络。
    - 推荐值： **1 到 3 步**。传统 Diffusion 要 50 步，这里因为用了蒸馏，3 步足矣。
        
2. **Guidance Weights
    - 定义： 听文本的话多一点，还是听动作（骨骼点）的话多一点。
    - 注意： 在 Student 推理阶段，这些权重已经被**蒸馏（Bake）**进模型参数里了，**不需要实时调节**，也不需要计算两遍，直接跑一次就行。

三、评估指标
1. PSNR - 衡量生成图像和真实图像的像素级差异。
2. SSIM 衡量两张图像在亮度、对比度和结构上的相似性。
3. LPIPS 使用预训练的 CNN 或 Transformer 提取特征，然后比较生成图像和真实图像的特征差异。
4. EPE（End-Point Error）：computed as the L2 distance between visible input tracks and the tracks extracted from the generated videos.
5. 貌似没看到衡量实时性的指标

四、最终效果：
1. 视频生成速度：achieves 17 FPS 每秒17帧的生成速度 at 480P and 10 FPS每秒十帧的速度 at 720P resolutions with sub-second 小于一秒的交互延迟latency on a single H100 GPU 。学生模型reaching 29 FPS when optimized with efficient VAE decoders that we specifically train for streaming applications. 【老师模型只能到0.79fps】【tiny VAE】
2. 实时交互延迟："...improving Wan 2.1 from 16.7 FPS with 0.69s latency to **29.5 FPS with 0.39s latency**, and Wan 2.2 from 10.4 FPS with 1.1s latency to **23.9 FPS with 0.49s latency**..."


> [!NOTE] VAE decoder
> 它的作用是把潜在空间（latent space）里的向量还原成图像或视频帧。

![[学习笔记/png/image-21.png]]

3 总结一下和手语生成能挂上钩的 重点理解
- 老师学生模式对实时生成有帮助
- 需不需要轨迹引导？



## 一些问题解答
1. 老师模型为什么是使用gt的noisy latent训练但是学生模型是使用自己生成的东西\老师的中间态来训练？
	1. 教师模型需要知道，在任何一个时间点 t，给定一个真实的图像加上确定量的噪声后，它应该往哪个方向去噪。
	2. 为了学习真实的“数据分布”，教师必须站在**“上帝视角”**的绝对正确路径上。前向过程（加噪）是纯数学的、完美的。教师模型通过看（真实图像 + 完美高斯噪声），学习如何一步一步精准地退回真实图像。如果给教师看错误的东西，它学到的物理规律就是歪的。
	3. 学生模型是一步生成模型，原本老师需要走 50 步去噪，学生希望能 4 步甚至 1 步走完。**如果在推理时：** 学生模型是从纯噪声开始，自己走第一步。由于模型不完美，第一步走完后，得到的 Latent 已经**略微偏离了完美的真实轨迹**。到了第二步、第三步，误差会像滚雪球一样越来越大（所谓的“一步错，步步错”）。**如果在训练时：** 如果学生模型只看完美的 Noisy Latent，它就像一个在模拟器里开车的司机，从来没偏离过车道。一旦在实战（推理）中稍微偏离了车道，它就不知道怎么把车拉回来了。**解决方案：** 让学生模型在训练时，直接看**自己上一阶段生成的、带有瑕疵的 Latent**。这样它就能学会在偏离完美轨迹时，如何强行把结果拉回到最终的真实图像上。

> [!NOTE] Self-forcing Self roll-out
> Contents

2. 目前的生成模型训练阶段会采用自己过去生成的东西吗？
	1. **绝对会。不仅会，而且这是目前实现长视频、无限长视频以及高一致性视频的核心机密之一。**
	2. **Autoregressive Conditioning（自回归条件注入）** 或者 **Context/Trajectory Augmentation（上下文/轨迹增强）**
	3. 当你让大模型生成一段 1 分钟的视频时，它的显存是不够一次性生成 1 分钟的。它必须先生成前 5 秒，然后把这 5 秒作为“条件（Condition）”，去生成下一个 5 秒。**训练时的痛点：** 训练时，如果我们总是拿“真实的过去 5 秒”去预测“真实的未来 5 秒”，模型会学得很好。但一旦到了**真实推理**阶段，模型拿来做条件的“过去 5 秒”是它**自己刚刚生成的**，肯定带有 AI 生成的瑕疵（比如轻微的闪烁、模糊、物理不连贯）。。
	4. Wan采用的方法是sliding window deque结构。每次放w个token，denoise之后出栈并且cache，然后下w个token进来denoise的时候会参考之前cache的token。




# DynamiCrafter
![[image-26.png]]
要解决的问题：如何将图片信息给到已经学会使用文本生成视频的模型。
1. 保留图片的语义信息
2. 保留图片的像素级视觉细节
### 1. 图像输入：双流注入机制 (Dual-stream Image Injection)
#### 文本对齐的上下文表示 (Text-aligned Context Representation)
- 使用**CLIP** 和文字embedding对齐。
	但是生成的图像的传统语义token mainly represents the visual content at the semantic level and **fails to capture the image’s full extent.**（信息不完整）
> 所以提取了 CLIP Image ViT 最后一层的**所有视觉 token F_vis**保留完整的图像信息

- **Query Transformer 转换：** 为了让这些视觉特征适应扩散模型并与文本对齐，作者引入了一个可学习的 **Query Transformer**（包含交叉注意力和前馈网络）。它将F_vis转换为最终的上下文特征F_ctx

#### 视觉细节引导 (Visual Detail Guidance, VDG)
上述处理还是会让图像细节流逝
- **通道级拼接 (Concatenation)：** 将输入的条件图像（经过 VAE 编码后的 Latent 特征）与模型每一帧的初始随机噪声（zt）在**通道维度上进行拼接（Concatenate）**。
    
- **直接输入 U-Net：** 拼接后的特征直接送入降噪 U-Net 的第一层。这确保了生成的视频在视觉上与原图保持高度一致（Visual Conformity）。


### 2. 文本输入：纯粹的动态控制 (Text for Dynamics)

- **文本编码：** 文本提示（Text Prompt）通过预训练的 CLIP Text Encoder 提取出文本嵌入特征F_txt。
- **解耦的动作控制（Decoupled Training）：** 论文在第 5 节特别提到，既然图像已经提供了“长什么样”（外观/场景）的信息，**文本的作用就应该专注于“怎么动”（动作/动态）**。为此，作者专门清洗并重新标注了一个数据集（剔除了冗长的场景描述，只保留如 "Man doing push-ups" 这样纯粹的动作词汇），让文本专门作为动作的控制条件。
> 对于手语生成有一个难点就是怎么让模型根据输入的prompt自己知道去翻译这些句子，然后自己学会动作控制？毕竟输入是没有办法给到动作控制的！


### 3. 图文多模态融合：双交叉注意力机制 (Dual Cross-Attention)
作者在 U-Net 的空间层设计了 **Spatial Dual-attn Transformer**，使用了一种带门控的交叉注意力机制：

- 模型分别计算目标特征与**文本特征**的 Cross-Attention，以及与**图像上下文特征**的 Cross-Attention。
- **自适应融合（tanh gating）：** 这两部分结果并不是简单相加，而是通过一个可学习的参数进行加权融合。  
    公式可以简化理解为：
    ```
    Fout=Attention(Text)+λ⋅Attention(Image_Context)
    ```
    



# DiT
![[image-27.png]]


LDM：
- **训练一个 VAE（自动编码器）**
    - Encoder E：把图像压缩成 latent
    - Decoder D：把 latent 解码回图像
        
- **在 latent 空间训练扩散模型**
    - 扩散模型不再处理像素，而是处理 latent 表示 z=E(x)
这篇论文也是在latent上训练的！


1. classifier-free guidance:最终的噪声预测 = 无条件预测 + s ×（条件预测 − 无条件预测.s就是控制条件引导的强度。
2. latent space apply DiT
3. 1. patchify:首先会把noizy latent进行patchify，变成一个token序列，长度为（I/p）^2简单理解就是分成小块，再把每个patch展平并映射到d维空间。再加入位置编码送入transformer
> p 越小 → patch 越细 → token 越多 → 模型越贵但效果更好 
> p 越大 → patch 越粗 → token 越少 → 模型更便宜但效果下

4. DiT block：AdaLN 不直接学习 LayerNorm 的 γ 和 β，而是让一个小 MLP 根据 (t 的 embedding + c 的 embedding) 来预测 γ 和 β。- γ（scale）和 β（shift）不再是固定参数，而是 **由条件信息动态生成**
5. 最终使用 adaLN‑Zero = 用 (t + c) 生成 γ、β、α，其中 α 初始为 0，使整个 Transformer block 初始等于 identity，训练更稳定。随着训练的进行MLP更新参数 alpha不再是0.




# Wan 
The core design of Wan is inspired by the success of **Diffusion Transformers (DiT)** (Peebles & Xie, 2023) combined with **Flow Matching** (Lipman et al., 2022), a framework that has demonstrated the significant performance gains achievable through scaling in both text to-image (T2I) (Esser et al., 2024) and text-to-video (T2V) (Kong et al., 2024) tasks.

> [!NOTE] FLOW Matching
> 生成模型本质上是学习一个“水流（向量场）”，把纯噪声（水滴）顺着水流推演成真实的图片。FM 的思想就是直接让神经网络去拟合（回归）这个目标向量场。

![[Pasted image 20260313125726.png]]
1. 3D **causal** VAE architecture：时间维度下4被，空间维度下八倍【127M】训练从2D到3D 从低清到高清。inference阶段采用什么技巧来控制不让显存爆炸？
	1. 每4帧作为一个chunk
	2. feature cache： 用cache来保存上一个cache的几帧，用一个卷积窗口来计算下一帧
![[image-30.png]]


> [!训练阶段的衡量指标]
> 1. 训练阶段衡量指标： PSNR 生成图像（或压缩图像）和原始图像之间的差异有多小和(frame per unit latency (second)) 
> 2. FVD（Frechet Video Distance）生成视频分布 vs 真实视频分布
> 3. CLIP Score / CLIP-SIM: 文本 ↔ 视频内容是否匹配 text embedding
frame embedding 再用cos计算similarity计算的是语义匹配度


1. DiT部分设计：
	1. pachify:（1，2，2）3D卷积再flatten成（Batch，Lenth，D）L=(1 + T/4) × H/16 ×W/16
	2. cross attention 导入文字条件
	3. MLP来embeding timestep adaLN
		1. ![[image-33.png|]]
	
![[image-31.png]]
![[image-32.png]]
2. Prompt alignment：First, we augment each video with a variety of captions, thereby increasing diversity. Second, we rewrite user prompts to align them with the distribution of video captions from the training stage. In the following, we detail these strategies.
3. 生成的视频的衡量指标：（WanBench）
	手语生成可以考虑
	- **ID Consistency：**
		- frame1 → DINO → feature1  
		- frame2 → DINO → feature2  
		- frame3 → DINO → feature
		- 计算帧之间的特征相似度，高度相似就说明是同一个物体
	- Smoothness
		用多模态模型分析视频。论文使用：- **Qwen2-VL**通过 **video question answering** 的方式判断：
		- 是否存在动作断裂
		- 是否有跳帧
		- 是否有奇怪变形
		- 动作是否连续
>对于手语生成还需要考虑一下语义是否正确. 生成的视频用我们自己的模型来识别,看看效果...

4. 多模态输入:图片+文字
	1. ![[image-28.png]]
	2. 条件照片 condition image with zero filled frames 对齐视频时间 **一张图片扩展成视频序列** 再经过这个wan vae压缩成latent
	3. binary mask 控制保留哪些帧:M ∈ {0,1}^{1×T×h×w}[rearrenge]
	4. 将noisy latent 和condition latent 还有mask在channel axis 上concate
	5. 另一方面condition image会通过一个CLIP得到emmbedding和text embedding通过decoupled cross attention \[image 和text信息不会混合在一起]




# 手语生成论文相关工作
![[Pasted image 20260313144045.png]]
传统方法中间过程越多，错误就会越来越多！glossno
1. 根据文字生成手语视频的工作：
	1. SignGAN (“Everybody Sign Now”，2020)：先使用a transformer architecture with a Mixture Density Network (MDN)来根据spoken language 采用Transformer+MDN预测骨骼，再用GAN合成真人手语视频。![[Pasted image 20260313134734.png]]
	2. **SignGen (Qi _et al._ 2024, ECCV)**:基于**潜在扩散 (latent diffusion)** 的文本2视频模型。直接从文字编码（CLIP文本+视频token）翻译生成手语视频，省去了中间显式姿态步骤。使用三个阶段Autoencoder与UNet扩散器。代码开源。![[Pasted image 20260313143220.png]]
		1. 预测噪声 损失函数 为啥不用FlowMatching？
		2. Multi-modal Conditions：
			1. text condition：OpenCLIP ViT H/14 [
			2. motion condition：
				1. optical flow：encodes the direction and magnitude of motion over time at a pixel-level granularity. We extract this information using a pre-trained I3D model
				2. depth sequence：深度信息能提供准确额的空间布局重建（解决手部遮挡？）pre-trained DPT [37] model
				3. pose sequence：finer-grained spatial control, we utilize pose informa tion as an additional conditioning input 手指细节（dwpose提取关键点）
				4. MCF module （Motion condition fusion）：![[Pasted image 20260313150801.png]]
					1. FlashAttention 做时序 self-attention 解决长时间依赖问题
					2. 最后是和video latent concatenate
			3. Motion-aware Sign Video Generation：
				1. Spatio-Temporal-Semantic Attention：让生成的帧数之间不是独立生成的，同时要能和文本对齐。spatial block 单帧内部做self attention。temporal block拿到的是video latent，对于同一个空间位置**across frames** 作为序列来做attention
			
				2. Fully Semantic-frame Interaction：不能让每一帧独立生成，要互相参考。于是选择将所有帧拼接成一张大图，做self attention。之后将text tokens 和video features做cross attention
			4. Video Frame Interpolation解决flickering
	3. 
			1. 
	4. Sign-SAM：直接在连续时空域合成手语动作，无需gloss。提出**“时序尺度感知调制”**模块融合多尺度时域特征，捕捉细节动作![[Pasted image 20260313142654.png]]
2. gloss到视频：Stoll 等人的 SignSynth（2020）首次实现从**手语词汇表**到高质量视频的端到端生成；“NaturalSigner”（ICLR 2024投稿）使用gloss辅助输入，也利用扩散模型显著提升了BLEU/FID等指标；SignViP（2025）采用离散化多条件Token方式，实现高保真视频；亚马逊的 DiffSign（2024）将2D姿态通过控制扩散（ControlNet）转换为定制签名人视频。
3. 扩散模型方法：寒亭等人的 SignSAM（2026）采用时空U-Net直接从文本生成细腻动作
4. 3D层面训练模型再生成：Baltatzis 等人的 Neural Sign Actors（CVPR 2024）在3D（SMPL-X）层面训练扩散模型以生成真实的3D手语动作
5. 工业界也有如AWS的 GenASL（2024）将语音/文本翻译为ASL虚拟形象，以及英国Silence Speaks初创（2025）构建了能够将英文文本翻译为BSL动画形象的系统。总体来看，最新工作中扩散模型占据主流，可生成更连贯、真实的动作；评估指标则结合了机器翻译指标（BLEU/ROUGE）、动作一致性（PCK、MPJPE）、视频质量（SSIM、FID/FVD）和用户研究来综合衡量。此外，常用数据集包括德国天气预报的 RWTH-PHOENIX-2014/T，中文手语集 CSL，美国ASL的 How2Sign（80小时多视角视频）等。以下给出主要方法的对比表格，并讨论常用数据集、评测协议、开源资源以及未来方向。




# SignVIP
总体思想是把生成任务看作是GPT回答翻译问题，把输入的text作为一种语言，把目标手语动作看做目标语言。要做到这点就需要构建基于动作的词表。多条件离散token扩散模型。使用VQ-VAE将每帧图像编码为离散tokens，结合文本-签名对应关系训练Transformer，最后用扩散模型逐步还原视频 引入**多视角条件**（额外的深度图、3D手骨骼细节）以提升细粒度表现。![[Pasted image 20260313141025.png]]
		1. 找到一种方法，把极其复杂的高维特征（细粒度的全身姿态 + 3D手部模型），变成像自然语言一样的离散 Token。
			1. **特征提取与融合（Multi-Condition Encoder）**：将真实视频提取出来的细粒度姿态和3D手部特征通过一个网络下采样拿到embedding
			2. **FSQ 量化压缩**：极其复杂的全身和手部动作，被强制压缩到离散词汇表（Vocabulary）中。得到了离散的token。![[image-1.png]]貌似实际代码是使用VQ-VAE。![[image.png]]
			3. 训练一个解码器 D_FSQ，将离散的 Token 还原回连续的嵌入向量。这一步使用 L2 重建损失函数进行训练，确保压缩成 Token 后，动作信息没有丢失。
		2. 把人类输入的**口语文本**，准确地“翻译”成第一部分设计出来的**离散动作 Token**。
			1. **基于 GPT-2 的自回归架构**：使用CLIP提取输入文本的语义特征**将其作为 GPT-2 模型的初始隐藏状态**。每一帧得的token基于上一帧生成的token。这里训练的gt是FSQEncoder编码的每一帧的token
			2. **多预测头（Multiple parallel heads）**: 基于当前的隐藏状态，一次性并行预测出下一帧所有的动作 Tokens。【每一帧会有多个token】然后，使用一个 **Mixer（混合器）**（由线性层、LayerNorm 和 GELU 组成），把刚预测出的这一帧的所有 Tokens 融合压缩成一个新的隐藏状态，喂给 GPT 去预测下一帧。
			3. 动态决定视频长度 能预测出一个EOS类似的东西
		3. 基于翻译出来的连续动作嵌入向量和一张参考图，生成手语视频。
			1. **Denoising U-Net**加入**Temporal-Attention Layer)**
			2. **Reference Net**- 它提取人物的衣服、长相、背景等特征，并在主 U-Net 的空间自注意力层（Self-attention）中，通过**特征拼接（Spatial Concatenation）**的方式注入进去，保证生成的视频“长得像这个人”。
			3. **Condition Guider ：输入是从 FSQ Decoder 还原出来的**连续动作嵌入向量。它将动作特征提取后，通过直接相加（Addition）的方式，注入到主 U-Net 的下采样特征图中，精确控制小人的骨骼姿态和手部动作。

自己复现提升性能的思路：
- [ ] 考虑使用更大的数据集训练
- [ ] VQ/FSQ词表考虑变大一点？目前是625.。
- [ ] 生成框架从Unet改成DiT。。不过为啥作者不用DiT啊？【手语视频数据量、大模型参数量很多，需要足够的手语视频样本。】可以考虑使用混元1.5
- [ ] 加速改进：consistency model [72] and efficient ODE solvers [36, 37],

生成模型这一块去噪使用Unet还是Dit？
为啥都是预测噪声不是像实时生成一样flow matching
这些论文对于实时有什么帮助？